[project]
name = "focus-prospeccion"
version = "0.3.0"
description = "MVP multi-cliente de prospeccion trazable para Focus Business"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
